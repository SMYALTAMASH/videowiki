
apiVersion: apps/v1
kind: Deployment
metadata:
    name: invitation-response-api-service-deployment
spec:
    replicas: 2
    selector:
        matchLabels:
            component: invitation-response-api-service
    template:
        metadata:
            labels:
                component: invitation-response-api-service
                stage: prod
        spec:
            containers:
            - name: invitation-response-api-service
              imagePullPolicy: Always
              image: example.dkr.ecr.eu-west-1.amazonaws.com/videowiki/invitation-response-api-service:master
              ports:
                - containerPort: 4000
              envFrom:
                - secretRef:
                    name: videowikisecretkeys
---

apiVersion: v1
kind: Service
metadata:
    name: invitation-response-api-service-cluster-ip
    # namespace: videowiki
        
spec:
    type: ClusterIP
    selector:
        component: invitation-response-api-service
    ports:
    - port: 4000
      targetPort: 4000    


