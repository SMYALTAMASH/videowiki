
apiVersion: apps/v1
kind: Deployment
metadata:
    name: audio-processor-service-deployment
spec:
    replicas: 2
    selector:
        matchLabels:
            component: audio-processor-service
    template:
        metadata:
            labels:
                component: audio-processor-service
                stage: prod
        spec:
            containers:
            - name: audio-processor-service
              imagePullPolicy: Always
              image: example.dkr.ecr.eu-west-1.amazonaws.com/videowiki/audio-processor-service:master
              ports:
                - containerPort: 4000
              envFrom:
                - secretRef:
                    name: videowikisecretkeys
---

apiVersion: v1
kind: Service
metadata:
    name: audio-processor-service-cluster-ip
    # namespace: videowiki
        
spec:
    type: ClusterIP
    selector:
        component: audio-processor-service
    ports:
    - port: 4000
      targetPort: 4000    


