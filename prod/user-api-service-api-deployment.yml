
apiVersion: apps/v1
kind: Deployment
metadata:
    name: user-api-service-deployment
spec:
    replicas: 2
    selector:
        matchLabels:
            component: user-api-service
    template:
        metadata:
            labels:
                component: user-api-service
                stage: prod
        spec:
            containers:
            - name: user-api-service
              imagePullPolicy: Always
              image: example.dkr.ecr.eu-west-1.amazonaws.com/videowiki/user-api-service:master
              ports:
                - containerPort: 4000
              envFrom:
                - secretRef:
                    name: videowikisecretkeys
---

apiVersion: v1
kind: Service
metadata:
    name: user-api-service-cluster-ip
    # namespace: videowiki
        
spec:
    type: ClusterIP
    selector:
        component: user-api-service
    ports:
    - port: 4000
      targetPort: 4000    


