
apiVersion: apps/v1
kind: Deployment
metadata:
    name: auth-service-deployment
spec:
    replicas: 2
    selector:
        matchLabels:
            component: auth-service
    template:
        metadata:
            labels:
                component: auth-service
                stage: prod
        spec:
            containers:
            - name: auth-service
              imagePullPolicy: Always
              image: example.dkr.ecr.eu-west-1.amazonaws.com/videowiki/auth-service:master
              ports:
                - containerPort: 4000
              envFrom:
                - secretRef:
                    name: videowikisecretkeys
---

apiVersion: v1
kind: Service
metadata:
    name: auth-service-cluster-ip
    # namespace: videowiki
        
spec:
    type: ClusterIP
    selector:
        component: auth-service
    ports:
    - port: 4000
      targetPort: 4000    


