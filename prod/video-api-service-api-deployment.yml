
apiVersion: apps/v1
kind: Deployment
metadata:
    name: video-api-service-deployment
spec:
    replicas: 2
    selector:
        matchLabels:
            component: video-api-service
    template:
        metadata:
            labels:
                component: video-api-service
                stage: prod
        spec:
            containers:
            - name: video-api-service
              imagePullPolicy: Always
              image: example.dkr.ecr.eu-west-1.amazonaws.com/videowiki/video-api-service:master
              ports:
                - containerPort: 4000
              envFrom:
                - secretRef:
                    name: videowikisecretkeys
---

apiVersion: v1
kind: Service
metadata:
    name: video-api-service-cluster-ip
    # namespace: videowiki
        
spec:
    type: ClusterIP
    selector:
        component: video-api-service
    ports:
    - port: 4000
      targetPort: 4000    


