
apiVersion: apps/v1
kind: Deployment
metadata:
    name: noise-cancellation-api-service-deployment
spec:
    replicas: 2
    selector:
        matchLabels:
            component: noise-cancellation-api-service
    template:
        metadata:
            labels:
                component: noise-cancellation-api-service
                stage: prod
        spec:
            containers:
            - name: noise-cancellation-api-service
              imagePullPolicy: Always
              image: example.dkr.ecr.eu-west-1.amazonaws.com/videowiki/noise-cancellation-api-service:master
              ports:
                - containerPort: 4000
              envFrom:
                - secretRef:
                    name: videowikisecretkeys
---

apiVersion: v1
kind: Service
metadata:
    name: noise-cancellation-api-service-cluster-ip
    # namespace: videowiki
        
spec:
    type: ClusterIP
    selector:
        component: noise-cancellation-api-service
    ports:
    - port: 4000
      targetPort: 4000    


