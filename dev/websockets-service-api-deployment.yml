
apiVersion: apps/v1
kind: Deployment
metadata:
    name: websockets-service-deployment
spec:
    replicas: 2
    selector:
        matchLabels:
            component: websockets-service
    template:
        metadata:
            labels:
                component: websockets-service
                stage: dev
        spec:
            containers:
            - name: websockets-service
              imagePullPolicy: Always
              image: websockets-service
              ports:
                - containerPort: 4000
              envFrom:
                - secretRef:
                    name: videowikisecretkeys
---

apiVersion: v1
kind: Service
metadata:
    name: websockets-service-cluster-ip
    # namespace: videowiki
        
spec:
    type: ClusterIP
    selector:
        component: websockets-service
    ports:
    - port: 4000
      targetPort: 4000    

    sessionAffinity: ClientIP


