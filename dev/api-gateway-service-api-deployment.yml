
apiVersion: apps/v1
kind: Deployment
metadata:
    name: api-gateway-service-deployment
spec:
    replicas: 2
    selector:
        matchLabels:
            component: api-gateway-service
    template:
        metadata:
            labels:
                component: api-gateway-service
                stage: dev
        spec:
            containers:
            - name: api-gateway-service
              imagePullPolicy: Always
              image: api-gateway-service
              ports:
                - containerPort: 4000
              envFrom:
                - secretRef:
                    name: videowikisecretkeys
---

apiVersion: v1
kind: Service
metadata:
    name: api-gateway-service-cluster-ip
    # namespace: videowiki
        
spec:
    type: ClusterIP
    selector:
        component: api-gateway-service
    ports:
    - port: 4000
      targetPort: 4000    


