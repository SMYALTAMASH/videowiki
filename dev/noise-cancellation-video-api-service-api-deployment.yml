
apiVersion: apps/v1
kind: Deployment
metadata:
    name: noise-cancellation-video-api-service-deployment
spec:
    replicas: 2
    selector:
        matchLabels:
            component: noise-cancellation-video-api-service
    template:
        metadata:
            labels:
                component: noise-cancellation-video-api-service
                stage: dev
        spec:
            containers:
            - name: noise-cancellation-video-api-service
              imagePullPolicy: Always
              image: noise-cancellation-video-api-service
              ports:
                - containerPort: 4000
              envFrom:
                - secretRef:
                    name: videowikisecretkeys
---

apiVersion: v1
kind: Service
metadata:
    name: noise-cancellation-video-api-service-cluster-ip
    # namespace: videowiki
        
spec:
    type: ClusterIP
    selector:
        component: noise-cancellation-video-api-service
    ports:
    - port: 4000
      targetPort: 4000    


