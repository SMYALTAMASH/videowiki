
apiVersion: apps/v1
kind: Deployment
metadata:
    name: email-service-deployment
spec:
    replicas: 2
    selector:
        matchLabels:
            component: email-service
    template:
        metadata:
            labels:
                component: email-service
                stage: dev
        spec:
            containers:
            - name: email-service
              imagePullPolicy: Always
              image: email-service
              ports:
                - containerPort: 4000
              envFrom:
                - secretRef:
                    name: videowikisecretkeys
---

apiVersion: v1
kind: Service
metadata:
    name: email-service-cluster-ip
    # namespace: videowiki
        
spec:
    type: ClusterIP
    selector:
        component: email-service
    ports:
    - port: 4000
      targetPort: 4000    


