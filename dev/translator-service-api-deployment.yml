
apiVersion: apps/v1
kind: Deployment
metadata:
    name: translator-service-deployment
spec:
    replicas: 2
    selector:
        matchLabels:
            component: translator-service
    template:
        metadata:
            labels:
                component: translator-service
                stage: dev
        spec:
            containers:
            - name: translator-service
              imagePullPolicy: Always
              image: translator-service
              ports:
                - containerPort: 4000
              envFrom:
                - secretRef:
                    name: videowikisecretkeys
---

apiVersion: v1
kind: Service
metadata:
    name: translator-service-cluster-ip
    # namespace: videowiki
        
spec:
    type: ClusterIP
    selector:
        component: translator-service
    ports:
    - port: 4000
      targetPort: 4000    


