
apiVersion: apps/v1
kind: Deployment
metadata:
    name: socket-connection-service-deployment
spec:
    replicas: 2
    selector:
        matchLabels:
            component: socket-connection-service
    template:
        metadata:
            labels:
                component: socket-connection-service
                stage: dev
        spec:
            containers:
            - name: socket-connection-service
              imagePullPolicy: Always
              image: socket-connection-service
              ports:
                - containerPort: 4000
              envFrom:
                - secretRef:
                    name: videowikisecretkeys
---

apiVersion: v1
kind: Service
metadata:
    name: socket-connection-service-cluster-ip
    # namespace: videowiki
        
spec:
    type: ClusterIP
    selector:
        component: socket-connection-service
    ports:
    - port: 4000
      targetPort: 4000    


